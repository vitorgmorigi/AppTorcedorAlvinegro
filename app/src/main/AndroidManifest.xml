<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apptorcedoralvinegro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaNoticia3"></activity>
        <activity android:name=".TelaNoticia2" />
        <activity android:name=".TelaNoticia1" />
        <activity android:name=".TelaNoticias" />
        <activity
            android:name=".TelaIntegrantes"
            android:label="@string/title_activity_tela_integrantes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TelaResultados" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>